# FairChange audit — 2026-09-09

## Scope

This audit covers the Python workflow, AgentCore payload boundary, persisted approval path, authentication claims, judging evidence, and final-submission requirements.

## Fixed before submission

- Free-form runtime prompts now become bounded ephemeral requests, so their evidence can be validated against the synthetic engagement instead of producing an unsupported request ID after model execution.
- Runtime payloads reject non-objects, empty or unknown fixture IDs, oversized prompts, fixture-ID reuse, and unsafe prompt IDs.
- Assessments now reject unknown decisions, unknown request IDs, empty summaries or evidence, malformed task policy fields, billable defects, and billable included revisions.
- The deterministic assessor rejects unknown request IDs instead of silently treating them as the scope-change fixture.
- Architecture, deployment, judging, and public-demo copy now distinguish Cognito runtime authentication from IAM deployment access and local HMAC test tokens.
- The video documentation now labels the checked-in clip as legacy and records the required end-to-end walkthrough and public YouTube/Vimeo constraint.

## Evidence verified

- Public repository and MIT license are present.
- GitHub Pages serves the synthetic fixture demo and the video asset.
- AgentCore runtime version 2 is configured with a Cognito custom JWT authorizer; an unauthenticated call returned 401 and a Cognito-authenticated `req-defect` call returned 200.
- The project has a balanced 20-case synthetic evaluation set and a persisted approval continuation.
- The audited test suite passes 18 tests; the offline workflow, restart cursor, staged owner/client gates, and 4-minute-36-second captioned walkthrough render were exercised after the fixes.

## Open gates

- Independent consultant sessions have not been run; no external validation is claimed.
- There is no unauthenticated browser proxy to the protected runtime. The Pages site is a read-only synthetic demo.
- The updated runtime code must be redeployed and re-smoked; the currently deployed version predates the prompt and validation hardening.
- The regenerated walkthrough must be uploaded publicly to YouTube or Vimeo, within five minutes.
- The Devpost project, track form, and final submission remain unsubmitted.
- The source environment still needs a clean dependency-backed test run before the audit can be marked complete.

These open items are submission gates, not hidden claims. They must be closed or explicitly disclosed before the final Devpost action.
